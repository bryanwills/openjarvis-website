---
import { site } from '../config/site';
import BrandDownloads from './BrandDownloads.astro';

const links = [
  { label: 'GitHub', href: site.links.github, ext: true },
  { label: 'Docs', href: site.links.docs, ext: true },
  { label: 'Discord', href: site.links.discord, ext: true },
  { label: 'Paper', href: site.links.paper, ext: true },
  { label: 'Releases', href: site.links.releases, ext: true },
];
---

<footer class="site-footer">
  <div class="container footer-inner">
    <BrandDownloads id="brand-dl-footer" placement="up">
      <img class="footer-mark" src="/logo-mark.svg" alt="" width="26" height="26" aria-hidden="true" />
      <span class="footer-brand-name">OpenJarvis</span>
    </BrandDownloads>

    <nav class="footer-nav" aria-label="Footer">
      {links.map((l, i) => (
        <>
          {i > 0 && <span class="footer-sep" aria-hidden="true">·</span>}
          <a href={l.href} {...l.ext ? { target: '_blank', rel: 'noopener noreferrer' } : {}}>{l.label}</a>
        </>
      ))}
    </nav>

    <p class="footer-copy">&copy; {new Date().getFullYear()} OpenJarvis Contributors · Apache-2.0</p>
  </div>
</footer>

<style>
  .site-footer {
    border-top: 1px solid transparent;
    margin-top: 4rem;
    padding: 3rem 0 2.5rem;
    background: var(--color-surface);
    position: relative;
  }
  .site-footer::before {
    content: '';
    position: absolute;
    left: 0;
    right: 0;
    top: 0;
    height: 1px;
    background: linear-gradient(90deg, transparent 8%,
      color-mix(in srgb, var(--color-holo) 38%, transparent) 50%,
      transparent 92%);
  }
  .footer-inner {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    gap: 1rem;
  }
  .footer-brand-name {
    font-family: var(--font-display);
    font-weight: 700;
    font-size: 1.1rem;
    color: var(--color-text);
  }
  .footer-mark { width: 26px; height: 26px; filter: drop-shadow(0 0 8px var(--logo-glow)); }

  .footer-nav {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    font-size: 0.92rem;
  }
  .footer-nav a { color: var(--color-text-secondary); font-weight: 500; }
  .footer-nav a:hover { color: var(--color-accent); }
  .footer-sep { color: var(--color-border); }

  .footer-copy {
    margin: 0;
    font-size: 0.8rem;
    color: var(--color-text-tertiary);
  }
</style>
